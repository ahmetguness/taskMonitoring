import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  root: {
    flex: 1,
    flexDirection: "row",
  },
  innerContainer: {
    height: "100%",
    width: "50%",
    justifyContent: "center",
    alignItems: "center",
  },
  text: {
    fontSize: 42,
    fontWeight: "bold",
  },
});
